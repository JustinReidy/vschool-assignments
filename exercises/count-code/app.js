let count = 0;

function countCode(arr){
    const newArr = []

    for(let i = 0; i < arr.length; i++){
        newArry.push(arr[i].split(""))
        for(let j = 0; j < newArr.length; j++){
            
        }
        if (arr[i] === "code"){
            count++;
        }
    }
    return count;
}

console.log(countCode(["code", "code", "code"]))